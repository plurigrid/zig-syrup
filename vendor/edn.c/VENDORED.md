Vendored from https://github.com/DotFox/edn.c (MIT, see LICENSE; src/ryu carries its own LICENSE).
Contents: include/ src/ (reader+writer) + bench/data + docs *.edn as the round-trip corpus for tools/edn_roundtrip.zig.
